import { styled } from 'stitches.config';

const HeroContainer = styled('div', {
  padding: '192px 0',

  textAlign: 'center',
  '@bp2': {
    padding: '274px 0',
  },
});

export default HeroContainer;
