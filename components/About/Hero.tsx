import AboutHero from 'components/AboutHero';

const Hero = ({ title, subtitle, imagePath }) => {
  return <AboutHero title={title} subtitle={subtitle} imagePath={imagePath} />;
};
export default Hero;
